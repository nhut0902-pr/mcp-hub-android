import MaterialIcons from "@expo/vector-icons/MaterialIcons";
import { useRouter } from "expo-router";
import { useState } from "react";
import { Alert, Linking, Modal, Pressable, ScrollView, StyleSheet, Switch, Text, View } from "react-native";

import { AppButton, Card, EmptyState, FormInput, palette, SectionTitle, StatusPill } from "@/components/hub-ui";
import { ScreenContainer } from "@/components/screen-container";
import { mcpCredentialHint } from "@/lib/mcp-hub/mcp-auth";
import { mcpCatalog, McpCatalogEntry } from "@/lib/mcp-hub/mcp-catalog";
import { useHub } from "@/lib/mcp-hub/context";
import { McpApiKeyHeader, McpAuthMode, McpConnectionResult, McpServerConfig, McpTransport, mcpApiKeyHeaderLabel, mcpAuthLabel, mcpTransportLabel } from "@/lib/mcp-hub/types";

const transports: McpTransport[] = ["streamable-http", "sse", "stdio"];
const authModes: McpAuthMode[] = ["none", "api-key", "oauth"];
const apiKeyHeaders: McpApiKeyHeader[] = ["authorization", "x-api-key"];

export default function McpScreen() {
  const router = useRouter();
  const { state, saveMcpServer, removeMcpServer, toggleMcpServer, testMcpServer, testingMcpId } = useHub();
  const [editing, setEditing] = useState<McpServerConfig | null>(null);
  const [visible, setVisible] = useState(false);
  const [name, setName] = useState("");
  const [transport, setTransport] = useState<McpTransport>("streamable-http");
  const [endpoint, setEndpoint] = useState("");
  const [command, setCommand] = useState("");
  const [args, setArgs] = useState("");
  const [authMode, setAuthMode] = useState<McpAuthMode>("none");
  const [apiKeyHeader, setApiKeyHeader] = useState<McpApiKeyHeader>("authorization");
  const [apiKey, setApiKey] = useState("");
  const [oauthToken, setOauthToken] = useState("");
  const [oauthIssuer, setOauthIssuer] = useState("");
  const [oauthClientId, setOauthClientId] = useState("");
  const [oauthScopes, setOauthScopes] = useState("");
  const [saving, setSaving] = useState(false);

  const openForm = (server?: McpServerConfig) => {
    setEditing(server ?? null);
    setName(server?.name ?? ""); setTransport(server?.transport ?? "streamable-http");
    setEndpoint(server?.endpoint ?? ""); setCommand(server?.command ?? ""); setArgs(server?.args ?? "");
    setAuthMode(server?.authMode ?? "none"); setApiKeyHeader(server?.apiKeyHeader ?? "authorization"); setApiKey(""); setOauthToken("");
    setOauthIssuer(server?.oauthIssuer ?? ""); setOauthClientId(server?.oauthClientId ?? ""); setOauthScopes(server?.oauthScopes ?? "");
    setVisible(true);
  };
  const applyPreset = (preset: McpCatalogEntry) => {
    setEditing(null); setName(preset.name); setTransport(preset.transport); setEndpoint(preset.endpoint); setCommand(""); setArgs(""); setAuthMode(preset.authMode); setApiKeyHeader(preset.apiKeyHeader ?? "authorization"); setApiKey(""); setOauthToken(""); setOauthIssuer(""); setOauthClientId(""); setOauthScopes(""); setVisible(true);
  };
  const showTestResult = (result: McpConnectionResult) => Alert.alert(result.title, result.detail);
  const save = async () => {
    if (!name.trim()) return Alert.alert("Thiếu thông tin", "Tên server MCP là bắt buộc.");
    if (transport === "stdio" ? !command.trim() : !endpoint.trim()) return Alert.alert("Thiếu thông tin", transport === "stdio" ? "Nhập command để lưu profile stdio." : "Nhập URL endpoint của MCP server.");
    if (authMode === "oauth" && !oauthToken.trim() && !editing?.oauthTokenStored) return Alert.alert("Cần OAuth access token", "Dán access token OAuth để dùng xác thực OAuth với URL MCP này.");
    const server: McpServerConfig = { id: editing?.id ?? `mcp-${Date.now()}`, name: name.trim(), transport, endpoint: endpoint.trim(), command: command.trim(), args: args.trim(), authMode: transport === "stdio" ? "none" : authMode, apiKeyHeader, apiKeyStored: editing?.apiKeyStored ?? false, oauthTokenStored: editing?.oauthTokenStored ?? false, oauthIssuer: oauthIssuer.trim(), oauthClientId: oauthClientId.trim(), oauthScopes: oauthScopes.trim(), connectionStatus: "checking", connectionDetail: null, lastCheckedAt: null, detectedServerName: null, enabled: editing?.enabled ?? true, updatedAt: new Date().toISOString() };
    setSaving(true);
    try { await saveMcpServer(server, apiKey, oauthToken); const result = await testMcpServer(server.id, server); setVisible(false); showTestResult(result); }
    catch (error) { Alert.alert("Không thể lưu MCP", error instanceof Error ? error.message : "Hãy thử lại."); }
    finally { setSaving(false); }
  };
  const testSavedServer = async (server: McpServerConfig) => { try { showTestResult(await testMcpServer(server.id)); } catch (error) { Alert.alert("Không thể kiểm tra MCP", error instanceof Error ? error.message : "Hãy thử lại."); } };
  const toggleWithFeedback = async (server: McpServerConfig, enabled: boolean) => { await toggleMcpServer(server.id, enabled); Alert.alert(enabled ? "Đã bật MCP" : "Đã tắt MCP", enabled ? `${server.name} sẽ được đưa vào ngữ cảnh chat. Kiểm tra kết nối trước khi dùng.` : `${server.name} không còn được đưa vào chat.`); };
  const confirmDelete = (server: McpServerConfig) => Alert.alert("Xoá server MCP", `Xoá cấu hình ${server.name}?`, [{ text: "Huỷ", style: "cancel" }, { text: "Xoá", style: "destructive", onPress: () => void removeMcpServer(server.id) }]);

  return <ScreenContainer edges={["top", "bottom", "left", "right"]}>
    <View style={styles.appHeader}>
      <Pressable testID="mcp-back" onPress={() => router.replace("/chat")} style={styles.backButton}><MaterialIcons name="arrow-back-ios-new" size={18} color={palette.text} /></Pressable>
      <Text style={styles.appTitle}>MCP Servers</Text>
      <View style={styles.headerSpacer} pointerEvents="none" />
    </View>
    <ScrollView contentContainerStyle={styles.content} showsVerticalScrollIndicator={false}>
      <View style={styles.header}><View><Text style={styles.title}>MCP server</Text><Text style={styles.subtitle}>Profile URL kèm API key hoặc OAuth khi cần.</Text></View><Pressable onPress={() => openForm()} style={({ pressed }) => [styles.addButton, { opacity: pressed ? 0.72 : 1 }]}><MaterialIcons name="add-link" size={20} color="#FFFFFF" /></Pressable></View>
      <View style={styles.note}><MaterialIcons name="info-outline" size={17} color={palette.navy} /><Text style={styles.noteText}>Lưu URL/API key/OAuth sẽ kiểm tra `initialize` ngay và báo rõ thành công, timeout, URL sai hoặc thiếu quyền. Secret chỉ lưu trên thiết bị.</Text></View>
      <SectionTitle title="Profile MCP" action={<Text style={styles.count}>{state.mcpServers.length} server</Text>} />
      {state.mcpServers.length ? state.mcpServers.map((server) => <Card key={server.id} style={styles.serverCard}><View style={styles.serverTop}><Pressable onPress={() => openForm(server)} style={styles.serverCopy}><View style={styles.icon}><MaterialIcons name={server.transport === "stdio" ? "terminal" : "lan"} color={palette.navy} size={18} /></View><View style={styles.serverText}><Text style={styles.serverName}>{server.name}</Text><Text style={styles.serverEndpoint} numberOfLines={1}>{server.transport === "stdio" ? server.command : server.endpoint}</Text></View></Pressable><Switch value={server.enabled} onValueChange={(value) => void toggleWithFeedback(server, value)} trackColor={{ false: "#6B6B6B", true: "#2996F3" }} thumbColor="#F8FAFC" /></View><View style={styles.serverMeta}><StatusPill label={mcpTransportLabel[server.transport]} tone="neutral" /><StatusPill label={mcpAuthLabel[server.authMode ?? "none"]} tone={server.authMode === "none" ? "neutral" : "success"} /><StatusPill label={connectionLabel(server.connectionStatus)} tone={connectionTone(server.connectionStatus)} /></View>{server.connectionDetail ? <Text style={styles.connectionDetail} numberOfLines={2}>{server.connectionDetail}</Text> : null}<View style={styles.actions}><AppButton label="Kiểm tra" icon="network-check" variant="primary" loading={testingMcpId === server.id} onPress={() => void testSavedServer(server)} /><AppButton label="Sửa" icon="edit" variant="secondary" onPress={() => openForm(server)} /><AppButton label="Xoá" icon="delete-outline" variant="danger" onPress={() => confirmDelete(server)} /></View></Card>) : <Card><EmptyState icon="hub" title="Chưa có MCP server" detail="Thêm profile HTTP, SSE hoặc stdio để lưu và kiểm tra kết nối của bạn." /></Card>}
      <SectionTitle title="MCP gợi ý" action={<Text style={styles.count}>{mcpCatalog.length} endpoint</Text>} />
      <Text style={styles.catalogIntro}>Các endpoint công khai đã kiểm tra tài liệu. Chạm Dùng để điền URL và chọn phương thức xác thực; OAuth có thể cần access token hoặc flow được dịch vụ cấp.</Text>
      {mcpCatalog.map((preset) => <Card key={preset.id} style={styles.catalogCard}><View style={styles.catalogCopy}><Text style={styles.catalogName}>{preset.name}</Text><Text style={styles.catalogDetail}>{preset.detail}</Text>{preset.readOnlyHint ? <Text style={styles.catalogHint}>{preset.readOnlyHint}</Text> : null}</View><View style={styles.catalogActions}><AppButton label="Dùng" icon="add-link" variant="secondary" onPress={() => applyPreset(preset)} /><AppButton label="Docs" icon="open-in-new" variant="ghost" onPress={() => void Linking.openURL(preset.docsUrl)} /></View></Card>)}
    </ScrollView>
    <Modal visible={visible} animationType="slide" presentationStyle="pageSheet" onRequestClose={() => setVisible(false)}><ScreenContainer edges={["top", "bottom", "left", "right"]}><View style={styles.modalHeader}><Pressable testID="mcp-form-back" onPress={() => setVisible(false)} style={styles.modalBack}><MaterialIcons name="arrow-back-ios-new" size={18} color={palette.text} /></Pressable><Text style={styles.modalTitle}>{editing ? "Sửa MCP server" : "MCP server mới"}</Text><Pressable disabled={saving} onPress={() => void save()} style={[styles.modalSave, saving && styles.disabled]}><Text style={styles.save}>{saving ? "Đang kiểm tra…" : "Lưu & kiểm tra"}</Text></Pressable></View><ScrollView contentContainerStyle={styles.form}><FormInput label="Tên server" value={name} onChangeText={setName} placeholder="Ví dụ: Filesystem team" /><Text style={styles.fieldLabel}>Transport</Text><View style={styles.optionGrid}>{transports.map((option) => <Pressable key={option} onPress={() => setTransport(option)} style={({ pressed }) => [styles.option, transport === option && styles.optionActive, { opacity: pressed ? 0.75 : 1 }]}><Text style={[styles.optionText, transport === option && styles.optionTextActive]}>{mcpTransportLabel[option]}</Text></Pressable>)}</View>{transport === "stdio" ? <><FormInput label="Command" value={command} onChangeText={setCommand} placeholder="npx hoặc node" autoCapitalize="none" /><FormInput label="Arguments" value={args} onChangeText={setArgs} placeholder="-y @modelcontextprotocol/server-filesystem /path" multiline autoCapitalize="none" /></> : <><FormInput label="Endpoint URL" value={endpoint} onChangeText={setEndpoint} placeholder="https://mcp.example.com/mcp" keyboardType="url" autoCapitalize="none" autoCorrect={false} hint={transport === "sse" ? "Nhập URL stream SSE của server." : "Nhập endpoint Streamable HTTP của server."} /><Text style={styles.fieldLabel}>Xác thực cho URL MCP</Text><View style={styles.optionGrid}>{authModes.map((option) => <Pressable key={option} onPress={() => setAuthMode(option)} style={({ pressed }) => [styles.option, authMode === option && styles.optionActive, { opacity: pressed ? 0.75 : 1 }]}><Text style={[styles.optionText, authMode === option && styles.optionTextActive]}>{mcpAuthLabel[option]}</Text></Pressable>)}</View>{authMode === "api-key" ? <><Text style={styles.fieldLabel}>Header API key</Text><View style={styles.optionGrid}>{apiKeyHeaders.map((option) => <Pressable key={option} onPress={() => setApiKeyHeader(option)} style={({ pressed }) => [styles.option, apiKeyHeader === option && styles.optionActive, { opacity: pressed ? 0.75 : 1 }]}><Text style={[styles.optionText, apiKeyHeader === option && styles.optionTextActive]}>{mcpApiKeyHeaderLabel[option]}</Text></Pressable>)}</View></> : null}<View style={styles.authHint}><MaterialIcons name="security" size={16} color={palette.navy} /><Text style={styles.authHintText}>{mcpCredentialHint(authMode, apiKeyHeader)}</Text></View>{authMode === "api-key" ? <FormInput label="API key" value={apiKey} onChangeText={setApiKey} placeholder={editing?.apiKeyStored ? "Đã lưu an toàn — nhập để thay thế" : "Dán API key"} secureTextEntry autoCapitalize="none" autoCorrect={false} hint="API key được lưu trong kho bảo mật của thiết bị và sẽ được kiểm tra ngay khi lưu." /> : null}{authMode === "oauth" ? <><FormInput label="OAuth issuer / authorization URL" value={oauthIssuer} onChangeText={setOauthIssuer} placeholder="https://auth.example.com" keyboardType="url" autoCapitalize="none" autoCorrect={false} hint="Lưu nguồn cấp quyền để nhận diện profile OAuth." /><FormInput label="OAuth client ID" value={oauthClientId} onChangeText={setOauthClientId} placeholder="mcp-mobile-client" autoCapitalize="none" autoCorrect={false} /><FormInput label="OAuth scopes" value={oauthScopes} onChangeText={setOauthScopes} placeholder="mcp.read mcp.write" autoCapitalize="none" autoCorrect={false} /><FormInput label="OAuth access token" value={oauthToken} onChangeText={setOauthToken} placeholder={editing?.oauthTokenStored ? "Đã lưu an toàn — nhập để thay thế" : "Dán access token OAuth"} secureTextEntry autoCapitalize="none" autoCorrect={false} hint="Dán token sau khi cấp quyền. Token được lưu bảo mật và kiểm tra khi lưu profile." /></> : null}</>}</ScrollView></ScreenContainer></Modal>
  </ScreenContainer>;
}

function connectionLabel(status: McpServerConfig["connectionStatus"]): string { return status === "connected" ? "Đã kết nối" : status === "checking" ? "Đang kiểm tra" : status === "auth-required" ? "Cần xác thực" : status === "failed" ? "Kết nối lỗi" : status === "unsupported" ? "Chỉ profile" : "Chưa kiểm tra"; }
function connectionTone(status: McpServerConfig["connectionStatus"]): "success" | "warning" | "neutral" { return status === "connected" ? "success" : status === "auth-required" || status === "failed" || status === "unsupported" ? "warning" : "neutral"; }

const styles = StyleSheet.create({
  appHeader: { height: 58, flexDirection: "row", alignItems: "center", justifyContent: "center", borderBottomWidth: 1, borderColor: "#303030" }, appTitle: { color: palette.text, fontSize: 16, fontWeight: "800" }, backButton: { position: "absolute", left: 13, width: 40, height: 40, alignItems: "center", justifyContent: "center" }, headerSpacer: { width: 40, height: 40, position: "absolute", right: 13 },
  content: { padding: 14, paddingBottom: 28, gap: 9 }, header: { flexDirection: "row", justifyContent: "space-between", alignItems: "center" }, title: { fontSize: 18, fontWeight: "800", letterSpacing: -0.4, color: palette.text }, subtitle: { color: palette.muted, fontSize: 11, marginTop: 3 }, addButton: { width: 40, height: 40, borderRadius: 12, backgroundColor: palette.navy, alignItems: "center", justifyContent: "center" }, note: { flexDirection: "row", alignItems: "flex-start", gap: 8, borderRadius: 12, backgroundColor: "#2C2C2C", padding: 10, marginTop: 4 }, noteText: { flex: 1, color: "#B8D9F2", fontSize: 10, lineHeight: 15 }, count: { color: palette.muted, fontSize: 10, fontWeight: "700" }, serverCard: { gap: 10 }, serverTop: { flexDirection: "row", alignItems: "center", gap: 9 }, serverCopy: { flex: 1, flexDirection: "row", alignItems: "center", gap: 9 }, icon: { width: 34, height: 34, borderRadius: 10, backgroundColor: palette.softNavy, alignItems: "center", justifyContent: "center" }, serverText: { flex: 1, gap: 1 }, serverName: { color: palette.text, fontSize: 14, fontWeight: "800" }, serverEndpoint: { color: palette.muted, fontSize: 10 }, serverMeta: { flexDirection: "row", alignItems: "center", gap: 7, flexWrap: "wrap" }, connectionDetail: { color: palette.muted, fontSize: 10, lineHeight: 14 }, actions: { flexDirection: "row", gap: 8, flexWrap: "wrap" }, catalogIntro: { color: palette.muted, fontSize: 10, lineHeight: 15, marginBottom: -2 }, catalogCard: { flexDirection: "row", alignItems: "center", gap: 10 }, catalogCopy: { flex: 1, gap: 3 }, catalogName: { color: palette.text, fontSize: 13, fontWeight: "800" }, catalogDetail: { color: palette.muted, fontSize: 10, lineHeight: 14 }, catalogHint: { color: "#9DCEF8", fontSize: 10, lineHeight: 14 }, catalogActions: { gap: 5, minWidth: 70 }, modalHeader: { minHeight: 54, paddingHorizontal: 14, flexDirection: "row", alignItems: "center", justifyContent: "center", borderBottomWidth: 1, borderBottomColor: palette.border }, modalBack: { position: "absolute", left: 10, width: 38, height: 38, alignItems: "center", justifyContent: "center" }, modalSave: { position: "absolute", right: 14, minWidth: 38, height: 38, alignItems: "center", justifyContent: "center" }, disabled: { opacity: 0.5 }, modalTitle: { color: palette.text, fontSize: 15, fontWeight: "800" }, save: { color: palette.navy, fontSize: 12, fontWeight: "800" }, form: { padding: 15, gap: 13 }, fieldLabel: { fontSize: 11, color: palette.text, fontWeight: "700", marginBottom: -7 }, optionGrid: { flexDirection: "row", flexWrap: "wrap", gap: 7 }, option: { paddingHorizontal: 10, paddingVertical: 8, borderRadius: 9, borderWidth: 1, borderColor: palette.border }, optionActive: { backgroundColor: palette.softNavy, borderColor: palette.navy }, optionText: { color: palette.muted, fontSize: 11, fontWeight: "700" }, optionTextActive: { color: "#83C6FF" }, authHint: { flexDirection: "row", gap: 7, alignItems: "flex-start", padding: 10, borderRadius: 10, backgroundColor: palette.softCyan }, authHintText: { flex: 1, color: "#BFE1FF", fontSize: 10, lineHeight: 15 },
});
